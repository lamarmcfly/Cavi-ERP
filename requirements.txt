# Cavi ERP — Python dependencies
# Pin majors; let patch/minor float for security fixes during scaffolding.

# Configuration
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
python-dotenv>=1.0,<2

# PostgreSQL (system of record + schema registry)
psycopg[binary]>=3.1,<4
psycopg-pool>=3.2,<4

# Redis (cache + event bus)
redis>=5.0,<6

# Vault — OS keyring credential storage
keyring>=24.0,<26

# Schema registry validation
jsonschema>=4.21,<5

# HTTP (talking to n8n / external systems)
httpx>=0.27,<1

# Dev / test  (lint/type/test gates — see pyproject.toml + .github/workflows/ci.yml)
pytest>=8.0,<9
ruff>=0.4,<1
mypy>=1.8,<2
fakeredis>=2.20,<3   # in-memory Redis for agent-runtime tests
