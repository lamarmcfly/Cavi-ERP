{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cavi:vault.secret.denied.v1",
  "title": "Vault Secret Denied",
  "description": "Emitted by Vault when a secret/token request is refused (policy, missing grant, expired credential).",
  "type": "object",
  "required": ["tenant_id", "erp_platform", "reason", "requested_at"],
  "additionalProperties": false,
  "properties": {
    "tenant_id": { "type": "string", "minLength": 1, "description": "Owning tenant. Never empty." },
    "erp_platform": { "type": "string", "minLength": 1, "description": "ERP the caller attempted to reach." },
    "reason": { "type": "string", "minLength": 1, "description": "Human-readable denial cause." },
    "requested_at": { "type": "string", "format": "date-time", "description": "ISO 8601 time the request was made." }
  }
}
