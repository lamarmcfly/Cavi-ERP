{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cavi:ticker.reconciliation.due.v1",
  "title": "Ticker Reconciliation Due",
  "description": "Emitted on the reconciliation cadence (scheduled through n8n) to trigger a Cavi-vs-ERP reconciliation pass for one tenant and record-type scope.",
  "type": "object",
  "required": ["tenant_id", "erp_platform", "scope", "due_at"],
  "additionalProperties": false,
  "properties": {
    "tenant_id": { "type": "string", "minLength": 1, "description": "Owning tenant. Never empty." },
    "erp_platform": { "type": "string", "minLength": 1, "description": "ERP to reconcile against." },
    "scope": { "type": "string", "minLength": 1, "description": "Record type to reconcile, e.g. journalEntry." },
    "due_at": { "type": "string", "format": "date-time", "description": "ISO 8601 timestamp the pass became due." }
  }
}
